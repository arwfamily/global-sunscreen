# global-sunscreen 적용 가이드 (2026-08-26, US 코퍼스 교체 반영)

---

## 먼저 읽어야 할 것 — 세라마이드 클레임이 무너졌어

새 미국 코퍼스(6,516개 선크림)로 다시 세니까 이렇게 나와:

- 세라마이드 함유 **312개**
- **3종 이상 세라마이드 106개**
- 콜레스테롤 함유 **212개**
- **3종+ 세라마이드 AND 콜레스테롤을 동시에 가진 제품 101개**

즉 **"미국 등록부 520개 중 완전 배리어 지질 세트 0개"는 옛 520개 피드가
아기·미네랄로 사전 필터된 집합이었기 때문에 나온 숫자**였어. 실제 제품:
No7 Restore & Renew (BCM 22개), Elizabeth Arden 21개, MDSolarSciences,
L'Oréal, DRMTLGY — 전부 **성인 페이스·안티에이징 선크림**이야.

**단, 클레임의 핵심은 살아 있어.** 아기 라벨 385개 중:

| | 개수 |
|---|---|
| 세라마이드 1종이라도 보유 | **1** |
| 3종 이상 세라마이드 | **0** |
| 콜레스테롤 | **0** |
| 아기 미네랄(168개) 중 위 조합 | **0** |

→ 쓸 수 있는 문장은 **"미국 등록부의 아기 선크림 385개(미네랄 168개 포함)
중 완전 배리어 지질 세트를 가진 제품은 0개"**로 좁혀야 해.
"미국 시판 선크림 전체에 전무"는 **더 이상 사실이 아니야** — Kolmar 자료나
브랜드 카피에 그 문장이 들어가 있으면 지금 고쳐야 해.

이건 네가 코퍼스를 넓힌 덕에 잡힌 거야. 옛 피드로는 영원히 몰랐을 오류고,
바이어나 기자가 먼저 찾아냈으면 브랜드 신뢰가 날아갔을 자리야.

---

## 두 번째로 급한 것 — 농도 단위가 유실되고 있어

`scripts/dailymed_scraper.py`가 `<numerator value="...">`만 읽고
**`unit=` 속성과 `<denominator>`를 안 읽어.** 그래서:

- 옥티살레이트 `50` → 실제로는 50 mg/g = **5%**
- 호모살레이트 `100`, `150` → **10%**, **15%**
- 징크옥사이드 `200` → **20%**

스코프 내 6,516개 중 **2,555개(39%)가 퍼센트로 읽으면 불가능한 값**이야.
그리고 `20`처럼 애매한 값은 20%인지 2%인지 **사후 복구가 불가능**해 —
파싱 시점에 잡아야 해.

**패치 전문을 `docs/upstream_unit_patch.md`에 정규식·헬퍼 함수까지 다
써놨어.** 그대로 붙여넣으면 돼. 검수 기준도 넣었어: 고친 뒤 아연 중앙값이
**20%** 근처로, 어떤 필터도 **25%** 초과가 없어야 정상이야.

그때까지 global-sunscreen 쪽 처리:
- `percent_w_w` = **null** (숫자를 지어내지 않음)
- `percent_estimate` + `estimate_basis`(`assumed_percent`/`assumed_mg_per_g`)
  = 탐색용 추정치, 가정이 붙어 있음
- **리포뮬레이션 감지는 영향 없음** — 원본 숫자를 그대로 비교하니까
  같은 제품의 스케일은 시간이 지나도 일정해
- 업스트림이 고쳐지면 코드 수정 없이 자동으로 진짜 %를 쓰기 시작해

---

## 새 코퍼스가 준 것 — UNII

새 데이터는 **성분마다 UNII(FDA 물질 코드)를 달고 있어.** 부형제 UNII
커버리지 **99%(146,839/147,602)**.

내 동의어 사전이 근사치로 하던 일을 정부 발급 ID가 대신해:

- 지문(fingerprint)의 비교 단위를 `U:<UNII>`로 바꿨어. 이름이 없을 때만
  `N:<정규화된 이름>`으로 내려가.
- FDA가 `.ALPHA.-TOCOPHEROL ACETATE` → `ALPHA-TOCOPHEROL ACETATE`로
  다시 써도 **리포뮬레이션으로 안 잡혀**. 반대로 이름은 그대로인데
  물질이 바뀌면 **잡혀.**
- 미국 코퍼스에서 이름↔UNII 5,311쌍을 추출해보니 호주 성분의 69%,
  캐나다 성분의 71%가 즉시 UNII로 해석돼 — 나중에 국가 간 조인을
  이름이 아니라 코드로 할 수 있는 길이 열렸어.

FDA 표기 관행도 정규화에 반영했어: `.ALPHA.` 그리스 문자 표기,
`, DL-` 입체화학 접미사, `C.I. 77492` 같은 컬러인덱스 단독 표기
(→ iron oxides로 해석, 예전엔 통째로 사라졌음).

---

## 스코프 — 6,516 / 13,285

UV필터 UNII로 그물을 던지면 **선크림이 아닌 것도 걸려.** 업스트림이
카테고리를 붙여놨고, 우리는 `sunscreen`만 남겼어:

| 제외 | 개수 |
|---|---|
| makeup (SPF 파운데이션·쿠션·틴티드) | 3,322 |
| other | 2,566 |
| lip_balm | 663 |
| diaper_cream | 181 |
| calamine | 37 |

전부 `data/canonical/us_excluded.jsonl`에 **이유와 함께** 남아. 캐나다에서
"sunburn"이 아연 연고·칼라민을 끌고 온 것과 같은 문제라, 같은 원칙으로
처리했어 — **본 적 없는 통과 보고 버린 통은 절대 같아 보이면 안 돼.**

넓히고 싶으면: `US_CATEGORIES=sunscreen,makeup,lip_balm`

---

## 지금 상태 (실측, 오늘 돌린 결과)

| | 제품 | 전성분 | 중앙값 | 타사와 처방 공유 |
|---|---|---|---|---|
| 🇺🇸 US | **6,516** | 6,467 | 22성분 | **818 (12.6%)** |
| 🇨🇦 CA | 1,910 | 1,896 | 27성분 | 46 (2%) |
| 🇦🇺 AU | 501 | 500 | 17성분 | 112 (22%) |
| 🇰🇷 KR | 대기 | | | |
| **합계** | **8,927** | 8,863 | | |

★ **미국 무필터 전량에서도 12.6%가 타사와 처방 공유** — 574개 클러스터,
그중 244개가 회사 경계를 넘어. 네 호주 "Same Formula, Different Label"
조사가 이제 미국 6,516개에서도 성립해. 이번엔 사전 필터가 없으니
**발표 가능한 숫자**야.

미국 시장 지형 (전부 무필터 기준, 처음 나온 숫자들):
- 미네랄 전용 **1,968 (30%)** / 화학필터 포함 4,505 / **숨은 부스터 1,650 (25%)**
- 아기 라벨 385개 중 미네랄 전용 **168 (44%)** — 호주 아기 29%, 한국 아기 67%와 비교
- 부틸옥틸살리실레이트 **24%** (옛 피드에선 0%로 나왔던 그 성분 — 이제 진짜 값)
- PHSA 14%(미네랄 내에선 40%), 실란코팅 17%, 페녹시에탄올 45%

---

## 할 일 순서

1. **파일 업로드** — `/mnt/user-data/outputs/global-sunscreen/` 구조 그대로.
   `core/` `tests/` `docs/`는 신규 폴더야.
2. **업스트림 단위 패치** — `docs/upstream_unit_patch.md` 대로
   `tinysafe-dailymed-scraper-v2` 수정 → 주간 런 1회 → 아연 중앙값 20% 확인.
3. **Actions 수동 실행** (`canada_full_rewalk` 켜고). 이번 런은 규칙 버전이
   올라가서(UNII 도입) **전 국가가 조용히 재기준선을 잡아** — 가짜 이벤트
   0건. 감시는 그다음 런부터.
4. **세라마이드 클레임 문장 교체** — 나가 있는 자료 전부. 새 문장:
   "미국 등록부 아기 선크림 385개 중 0개".
5. **한국 CSV**를 `data/raw/kr/`에.

매일 읽을 곳은 여전히 **`data/events/review_queue.md`** 하나야.
